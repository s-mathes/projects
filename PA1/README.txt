This program implements a linked list, wherein the user can add, remove, and search
for a number in a sorted list. Compile by using the command 'make', which will create the
executable 'main'.

Commands:
'i #' to insert a number
'd #' to delete a number
'p' to print the sorted list
's #' to search for a number
'x' to exit the program
